<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Emulador Android: 10.0.2.2 e o alias para o localhost da maquina host -->
        <domain includeSubdomains="false">10.0.2.2</domain>

        <!-- Dispositivo fisico: descomente e troque pelo IP da sua maquina na rede local. Precisa estar na MESMA rede Wi-Fi do PC, e o firewall do Windows pode precisar liberar a porta 8080. -->
        <!-- <domain includeSubdomains="false">192.168.0.42</domain> -->
    </domain-config>
</network-security-config>